# Rutgers

## Local interactive dashboard window

Run a local static server and open the interactive dashboard preview:

```bash
python3 -m http.server 3000
```

Then visit:

- `http://127.0.0.1:3000/dashboard-local.html`

This local page includes interactive module tabs, modal behavior, CSV export placeholder, and transcript placeholder controls for quick UI validation.
